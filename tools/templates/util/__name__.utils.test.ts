import { __method_name__ } from './__name__.utils';

describe('__name__.utils', () => {
  describe('__method_name__', () => {
    test('should return ...', () => {
      const actualResult = __method_name__();
      const expectedResult = null;

      expect(actualResult).toBe(expectedResult);
    });
  });
});
